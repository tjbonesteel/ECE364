#! /usr/bin/env python

from Tkinter import *
import sys

# All TK GUIs must have a root widget
# The root widget is the primary application window and also manages the event loop
Root = Tk()
Root.mainloop()

# Always remember to call mainloop() otherwise nothing will happen!

sys.exit(0)
